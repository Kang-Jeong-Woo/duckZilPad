interface Table {
    userId: String,
    contents: Object,
    pinned: Boolean,
    isDelete: Boolean,
    style: Object,
    color: Object,
    width: Number,
    height: Number,
    positionX: Number,
    positionY: Number,
    positionZ: Number,
}

export default Table